// Simple notification helper (placeholder)
export function sendNotification(message) {
  // Replace with real notification integration (email/push) as needed
  // For now, just log to console
  console.info('[Notification]', message);
}
